--[[
    RENXX HUB - BYPASS GAMEPASS
    By: DEEP & RENXX
    Version: 3.0 (FLUENT EDITION)
    Library: Fluent UI
    Type: Keyless
    Platform: PC & Mobile
]]

-- Load Fluent UI
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- ==================== SERVICES ====================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local RS = game:GetService("ReplicatedStorage")
local MarketplaceService = game:GetService("MarketplaceService")
local LP = Players.LocalPlayer
local Cam = workspace.CurrentCamera

-- ==================== CONFIG ====================
local C = {
    Bypass=false, BypassV2=false, SpeedBypass=false,
    AntiKick=false, NoDetect=false, BoostFPS=false,
    FreeGamepass=false, FreeGamepassV2=false, FreeGamepassV3=false,
    HighlightESP=false,
    TeamHighlightColor=Color3.fromRGB(0,255,0),
    EnemyHighlightColor=Color3.fromRGB(255,0,0),
    BotHighlightColor=Color3.fromRGB(255,255,0),
    Fullbright=false, NoFog=false, RemoveParticle=false,
    SkyColor=Color3.fromRGB(0,120,255),
    SpeedHack=false, SpeedValue=50,
    JumpHack=false, JumpValue=50,
    InfiniteJump=false, BunnyHop=false,
    NoClip=false, Fly=false, FlySpeed=50,
    SelectedPlayer=nil, FreezeBot=false,
}

local function isSameTeam(p)
    if not LP.Team or not p.Team then return false end
    return p.Team == LP.Team
end

-- ==================== FLUENT WINDOW ====================
local Window = Fluent:CreateWindow({
    Title = "RENXX HUB",
    SubTitle = "Bypass Gamepass v3.0 | By DEEP & RENXX",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Protection = Window:AddTab({ Title = "Protection", Icon = "shield" }),
    Gamepass = Window:AddTab({ Title = "Gamepass", Icon = "key" }),
    Visual = Window:AddTab({ Title = "Visual", Icon = "eye" }),
    Player = Window:AddTab({ Title = "Player", Icon = "user" }),
    Game = Window:AddTab({ Title = "Game", Icon = "gamepad-2" }),
}

local Options = Fluent.Options

-- ==================== MODULE HOOK SYSTEM ====================
local hookedModules = {}
local hookedRemotes = {}

local function hookAllModules()
    local count = 0
    for _, obj in pairs(RS:GetDescendants()) do
        pcall(function()
            if obj:IsA("ModuleScript") then
                local n = obj.Name:lower()
                if n:find("gamepass") or n:find("bypass") or n:find("marketplace")
                   or n:find("purchase") or n:find("premium") or n:find("vip")
                   or n:find("owned") or n:find("player") or n:find("character")
                   or n:find("setting") or n:find("config") then
                    local ok, data = pcall(require, obj)
                    if ok and type(data) == "table" then
                        hookedModules[obj] = data
                        count = count + 1
                    end
                end
            end
        end)
    end
    return count
end

local function hookAllRemotes()
    local count = 0
    for _, obj in pairs(RS:GetDescendants()) do
        pcall(function()
            if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
                local n = obj.Name:lower()
                if n:find("gamepass") or n:find("purchase") or n:find("buy")
                   or n:find("premium") or n:find("vip") or n:find("owned") then
                    hookedRemotes[obj] = obj
                    count = count + 1
                end
            end
        end)
    end
    return count
end

task.spawn(function()
    task.wait(2)
    local modCount = hookAllModules()
    local remCount = hookAllRemotes()
    print("[RENXX] "..modCount.." modules + "..remCount.." remotes hooked")
end)

-- ==================== PROTECTION TAB ====================
Tabs.Protection:AddSection("Bypass")

Tabs.Protection:AddToggle("Bypass", {
    Title = "Bypass",
    Default = false,
    Callback = function(v)
        C.Bypass = v
        if v then
            pcall(function()
                local mt = getrawmetatable(game)
                if mt and setreadonly then
                    setreadonly(mt, false)
                    local oldNC = mt.__namecall
                    mt.__namecall = newcclosure(function(self, ...)
                        local m = getnamecallmethod()
                        if m == "Kick" and self == LP then return nil end
                        return oldNC(self, ...)
                    end)
                    setreadonly(mt, true)
                end
            end)
        end
    end
})

Tabs.Protection:AddToggle("BypassV2", {
    Title = "Bypass V2",
    Default = false,
    Callback = function(v) C.BypassV2 = v end
})

Tabs.Protection:AddToggle("SpeedBypass", {
    Title = "Speed Bypass",
    Default = false,
    Callback = function(v) C.SpeedBypass = v end
})

Tabs.Protection:AddToggle("AntiKick", {
    Title = "Anti Kick",
    Default = false,
    Callback = function(v) C.AntiKick = v end
})

Tabs.Protection:AddToggle("NoDetect", {
    Title = "No Detect",
    Default = false,
    Callback = function(v)
        C.NoDetect = v
        if v and LP.Character then
            for _, p in pairs(LP.Character:GetDescendants()) do
                pcall(function()
                    if p:IsA("BasePart") then
                        p.CustomPhysicalProperties = PhysicalProperties.new(0.7, 0.3, 0.5)
                    end
                end)
            end
        end
    end
})

Tabs.Protection:AddToggle("BoostFPS", {
    Title = "Boost FPS",
    Default = false,
    Callback = function(v)
        C.BoostFPS = v
        if v then
            for _, obj in pairs(game:GetDescendants()) do
                pcall(function()
                    if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") then
                        obj.Enabled = false
                    end
                end)
            end
        end
    end
})

-- ==================== GAMEPASS TAB ====================
Tabs.Gamepass:AddSection("Tools")

Tabs.Gamepass:AddButton({
    Title = "🔍 Scan Gamepass (Module + Remote + Value)",
    Description = "Scan semua gamepass di ReplicatedStorage",
    Callback = function()
        local found = {modules={}, remotes={}, values={}}
        for _, obj in pairs(RS:GetDescendants()) do
            pcall(function()
                if obj:IsA("ModuleScript") then
                    local n = obj.Name:lower()
                    if n:find("gamepass") or n:find("pass") or n:find("premium") or n:find("vip") or n:find("owned") then
                        table.insert(found.modules, obj.Name)
                    end
                end
            end)
        end
        for _, obj in pairs(RS:GetDescendants()) do
            pcall(function()
                if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
                    local n = obj.Name:lower()
                    if n:find("gamepass") or n:find("pass") or n:find("purchase") or n:find("buy") or n:find("premium") then
                        table.insert(found.remotes, obj.Name)
                    end
                end
            end)
        end
        for _, obj in pairs(RS:GetDescendants()) do
            pcall(function()
                if obj:IsA("NumberValue") or obj:IsA("IntValue") or obj:IsA("BoolValue") then
                    local n = obj.Name:lower()
                    if n:find("gamepass") or n:find("pass") or n:find("premium") or n:find("vip") then
                        table.insert(found.values, obj.Name)
                    end
                end
            end)
        end
        local total = #found.modules + #found.remotes + #found.values
        if total > 0 then
            Fluent:Notify({
                Title = "RENXX HUB",
                Content = "Modules: "..#found.modules.." | Remotes: "..#found.remotes.." | Values: "..#found.values,
                Duration = 8
            })
            print("=== SCAN RESULT ===")
            for _, n in pairs(found.modules) do print("[Module] "..n) end
            for _, n in pairs(found.remotes) do print("[Remote] "..n) end
            for _, n in pairs(found.values) do print("[Value] "..n) end
        else
            Fluent:Notify({Title="RENXX HUB", Content="No gamepass found", Duration=5})
        end
    end
})

Tabs.Gamepass:AddSection("Free Gamepass")

Tabs.Gamepass:AddToggle("FreeGamepassV1", {
    Title = "Free Gamepass V1 (Module Hook)",
    Default = false,
    Callback = function(v)
        C.FreeGamepass = v
        if v then
            for mod, data in pairs(hookedModules) do
                pcall(function()
                    for k, val in pairs(data) do
                        local kn = tostring(k):lower()
                        if kn:find("gamepass") or kn:find("owned") or kn:find("premium") or kn:find("vip") or kn:find("pass") then
                            if type(val) == "boolean" then data[k] = true end
                            if type(val) == "number" then data[k] = 1 end
                        end
                    end
                end)
            end
            pcall(function()
                local mt = getrawmetatable(game)
                if mt and setreadonly then
                    setreadonly(mt, false)
                    local oldNamecall = mt.__namecall
                    mt.__namecall = newcclosure(function(self, ...)
                        local method = getnamecallmethod()
                        if method == "UserOwnsGamePassAsync" then return true end
                        return oldNamecall(self, ...)
                    end)
                    setreadonly(mt, true)
                end
            end)
            Fluent:Notify({Title="RENXX HUB", Content="Free Gamepass V1 aktif!", Duration=3})
        end
    end
})

Tabs.Gamepass:AddToggle("FreeGamepassV2", {
    Title = "Free Gamepass V2 (Remote Fire)",
    Default = false,
    Callback = function(v)
        C.FreeGamepassV2 = v
        if v then
            for remote, obj in pairs(hookedRemotes) do
                pcall(function()
                    if remote:IsA("RemoteEvent") then
                        remote:FireServer()
                        remote:FireServer(true)
                        remote:FireServer(1)
                    elseif remote:IsA("RemoteFunction") then
                        remote:InvokeServer()
                    end
                end)
            end
            for _, obj in pairs(RS:GetDescendants()) do
                pcall(function()
                    if obj:IsA("RemoteEvent") then
                        local n = obj.Name:lower()
                        if n:find("gamepass") or n:find("purchase") or n:find("buy") or n:find("premium") then
                            obj:FireServer()
                        end
                    end
                end)
            end
            Fluent:Notify({Title="RENXX HUB", Content="Free Gamepass V2 aktif!", Duration=3})
        end
    end
})

Tabs.Gamepass:AddToggle("FreeGamepassV3", {
    Title = "Free Gamepass V3 (Combo)",
    Default = false,
    Callback = function(v)
        C.FreeGamepassV3 = v
        if v then
            for mod, data in pairs(hookedModules) do
                pcall(function()
                    for k, val in pairs(data) do
                        local kn = tostring(k):lower()
                        if kn:find("gamepass") or kn:find("owned") or kn:find("premium") or kn:find("vip") then
                            if type(val) == "boolean" then data[k] = true end
                            if type(val) == "number" then data[k] = 1 end
                            if type(val) == "string" then data[k] = "owned" end
                        end
                    end
                end)
            end
            for remote, obj in pairs(hookedRemotes) do
                pcall(function()
                    if remote:IsA("RemoteEvent") then remote:FireServer(true) end
                end)
            end
            pcall(function()
                if getgenv then
                    getgenv().HasGamepass = true
                    getgenv().OwnsGamepass = true
                    getgenv().IsPremium = true
                end
            end)
            Fluent:Notify({Title="RENXX HUB", Content="Free Gamepass V3 aktif!", Duration=3})
        end
    end
})

-- ==================== VISUAL TAB ====================
Tabs.Visual:AddSection("Highlight ESP")

Tabs.Visual:AddToggle("HighlightESP", {
    Title = "Highlight ESP",
    Default = false,
    Callback = function(v) C.HighlightESP = v end
})

Tabs.Visual:AddColorPicker("TeamColor", {
    Title = "Team Highlight Color",
    Default = Color3.fromRGB(0,255,0),
    Callback = function(c) C.TeamHighlightColor = c end
})

Tabs.Visual:AddColorPicker("EnemyColor", {
    Title = "Enemy Highlight Color",
    Default = Color3.fromRGB(255,0,0),
    Callback = function(c) C.EnemyHighlightColor = c end
})

Tabs.Visual:AddColorPicker("BotColor", {
    Title = "BOT/NPC Highlight Color",
    Default = Color3.fromRGB(255,255,0),
    Callback = function(c) C.BotHighlightColor = c end
})

Tabs.Visual:AddSection("Lighting")

Tabs.Visual:AddToggle("Fullbright", {
    Title = "Fullbright",
    Default = false,
    Callback = function(v)
        C.Fullbright = v
        Lighting.Brightness = v and 2 or 1
        Lighting.ClockTime = v and 12 or 14
        Lighting.GlobalShadows = not v
    end
})

Tabs.Visual:AddToggle("NoFog", {
    Title = "No Fog",
    Default = false,
    Callback = function(v)
        C.NoFog = v
        Lighting.FogEnd = v and 100000 or 1000
    end
})

Tabs.Visual:AddToggle("RemoveParticle", {
    Title = "Remove Particle",
    Default = false,
    Callback = function(v)
        C.RemoveParticle = v
        if v then
            for _, obj in pairs(game:GetDescendants()) do
                pcall(function()
                    if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") then
                        obj.Enabled = false
                    end
                end)
            end
        end
    end
})

Tabs.Visual:AddColorPicker("SkyColor", {
    Title = "Sky Color",
    Default = Color3.fromRGB(0,120,255),
    Callback = function(c)
        C.SkyColor = c
        pcall(function()
            local sky = Lighting:FindFirstChildOfClass("Sky")
            if sky then
                sky.SkyboxBk = ""
                sky.SkyboxDn = ""
                sky.SkyboxFt = ""
                sky.SkyboxLf = ""
                sky.SkyboxRt = ""
                sky.SkyboxUp = ""
            end
            Lighting.Ambient = c
        end)
    end
})

-- ==================== PLAYER TAB ====================
Tabs.Player:AddSection("Movement")

Tabs.Player:AddToggle("SpeedHack", {
    Title = "Speed Hack",
    Default = false,
    Callback = function(v) C.SpeedHack = v end
})

Tabs.Player:AddSlider("SpeedValue", {
    Title = "Speed Multiplier",
    Default = 50,
    Min = 0,
    Max = 100,
    Rounding = 1,
    Callback = function(v) C.SpeedValue = v end
})

Tabs.Player:AddToggle("JumpHack", {
    Title = "Jump Hack",
    Default = false,
    Callback = function(v) C.JumpHack = v end
})

Tabs.Player:AddSlider("JumpValue", {
    Title = "Jump Power",
    Default = 50,
    Min = 0,
    Max = 100,
    Rounding = 1,
    Callback = function(v) C.JumpValue = v end
})

Tabs.Player:AddToggle("InfiniteJump", {
    Title = "Infinite Jump",
    Default = false,
    Callback = function(v) C.InfiniteJump = v end
})

Tabs.Player:AddToggle("BunnyHop", {
    Title = "Bunny Hop",
    Default = false,
    Callback = function(v) C.BunnyHop = v end
})

Tabs.Player:AddToggle("NoClip", {
    Title = "No Clip",
    Default = false,
    Callback = function(v) C.NoClip = v end
})

Tabs.Player:AddSection("Fly")

Tabs.Player:AddToggle("Fly", {
    Title = "Fly",
    Default = false,
    Callback = function(v) C.Fly = v end
})

Tabs.Player:AddSlider("FlySpeed", {
    Title = "Fly Speed",
    Default = 50,
    Min = 10,
    Max = 200,
    Rounding = 1,
    Callback = function(v) C.FlySpeed = v end
})

-- ==================== GAME TAB ====================
Tabs.Game:AddSection("Player")

local playerList = {}
for _, p in pairs(Players:GetPlayers()) do
    if p ~= LP then table.insert(playerList, p.Name) end
end

Tabs.Game:AddDropdown("SelectPlayer", {
    Title = "Select Player",
    Values = playerList,
    Default = "",
    Callback = function(o) C.SelectedPlayer = o end
})

Tabs.Game:AddButton({
    Title = "Teleport To Player",
    Callback = function()
        if C.SelectedPlayer then
            local target = Players:FindFirstChild(C.SelectedPlayer)
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") and LP.Character then
                LP.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(0,3,0)
            end
        end
    end
})

Tabs.Game:AddToggle("FreezeBot", {
    Title = "Freeze BOT/NPC",
    Default = false,
    Callback = function(v) C.FreezeBot = v end
})

-- ==================== HIGHLIGHT ESP LOOP ====================
local highlights = {}
RunService.RenderStepped:Connect(function()
    if not C.HighlightESP then
        for _, h in pairs(highlights) do pcall(function() h:Destroy() end) end
        highlights = {}
        return
    end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP and p.Character then
            local hl = p.Character:FindFirstChild("RENXXHighlight")
            if not hl then
                hl = Instance.new("Highlight")
                hl.Name = "RENXXHighlight"
                hl.Adornee = p.Character
                hl.Parent = p.Character
                table.insert(highlights, hl)
            end
            if isSameTeam(p) then hl.FillColor = C.TeamHighlightColor
            else hl.FillColor = C.EnemyHighlightColor end
            hl.OutlineColor = Color3.fromRGB(255,255,255)
            hl.FillTransparency = 0.5
        end
    end
    for _, obj in pairs(workspace:GetDescendants()) do
        pcall(function()
            if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") then
                if not Players:GetPlayerFromCharacter(obj) then
                    local hl = obj:FindFirstChild("RENXXBotHL")
                    if not hl then
                        hl = Instance.new("Highlight")
                        hl.Name = "RENXXBotHL"
                        hl.Adornee = obj
                        hl.Parent = obj
                        table.insert(highlights, hl)
                    end
                    hl.FillColor = C.BotHighlightColor
                    hl.OutlineColor = Color3.fromRGB(255,255,255)
                    hl.FillTransparency = 0.5
                end
            end
        end)
    end
end)

-- ==================== SPEED (FIXED) ====================
local speedBV = nil
RunService.RenderStepped:Connect(function()
    if not LP.Character then return end
    local h = LP.Character:FindFirstChildOfClass("Humanoid")
    local hrp = LP.Character:FindFirstChild("HumanoidRootPart")
    if h then
        if C.SpeedHack then
            h.WalkSpeed = C.SpeedValue
            if hrp then
                if not speedBV or speedBV.Parent ~= hrp then
                    if speedBV then speedBV:Destroy() end
                    speedBV = Instance.new("BodyVelocity")
                    speedBV.MaxForce = Vector3.new(math.huge, 0, math.huge)
                    speedBV.Velocity = Vector3.new(0,0,0)
                    speedBV.Parent = hrp
                end
                if h.MoveDirection.Magnitude > 0 then
                    speedBV.Velocity = h.MoveDirection * C.SpeedValue
                else
                    speedBV.Velocity = Vector3.new(0,0,0)
                end
            end
        elseif speedBV then
            speedBV:Destroy()
            speedBV = nil
        end
    end
end)

-- ==================== FLY (FIXED) ====================
local flyBV, flyBG
RunService.Heartbeat:Connect(function()
    if C.Fly and LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LP.Character.HumanoidRootPart
        if not flyBV then
            flyBV = Instance.new("BodyVelocity")
            flyBV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            flyBV.Parent = hrp
        end
        if not flyBG then
            flyBG = Instance.new("BodyGyro")
            flyBG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
            flyBG.P = 1000
            flyBG.Parent = hrp
        end
        local md = Vector3.new()
        if UIS:IsKeyDown(Enum.KeyCode.W) then md = md + Cam.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.S) then md = md - Cam.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.A) then md = md - Cam.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.D) then md = md + Cam.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.Space) then md = md + Vector3.new(0,1,0) end
        if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then md = md - Vector3.new(0,1,0) end
        flyBV.Velocity = md * C.FlySpeed
        flyBG.CFrame = Cam.CFrame
    else
        if flyBV then flyBV:Destroy() flyBV = nil end
        if flyBG then flyBG:Destroy() flyBG = nil end
    end
end)

-- ==================== HEARTBEAT ====================
RunService.Heartbeat:Connect(function()
    if LP.Character then
        local h = LP.Character:FindFirstChildOfClass("Humanoid")
        if h then
            if C.AntiKick and h.Health <= 0 then h.Health = h.MaxHealth end
            if C.JumpHack then h.UseJumpPower = true; h.JumpPower = C.JumpValue end
            if C.BunnyHop and h.MoveDirection.Magnitude > 0 then
                h:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
        if C.NoClip then
            for _, p in pairs(LP.Character:GetDescendants()) do
                pcall(function() if p:IsA("BasePart") then p.CanCollide = false end end)
            end
        end
    end
end)

-- ==================== INFINITE JUMP ====================
local function setupInfJump(char)
    if not char then return end
    local hum = char:WaitForChild("Humanoid", 5)
    if not hum then return end
    hum.StateChanged:Connect(function(old, new)
        if C.InfiniteJump and new == Enum.HumanoidStateType.Freefall then
            task.wait(0.05)
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end
LP.CharacterAdded:Connect(setupInfJump)
if LP.Character then setupInfJump(LP.Character) end

-- ==================== FREEZE BOT/NPC ====================
task.spawn(function()
    while task.wait(0.5) do
        if C.FreezeBot then
            for _, obj in pairs(workspace:GetDescendants()) do
                pcall(function()
                    if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") then
                        if not Players:GetPlayerFromCharacter(obj) then
                            local h = obj:FindFirstChildOfClass("Humanoid")
                            if h then h.WalkSpeed = 0; h.JumpPower = 0 end
                            local hrp = obj:FindFirstChild("HumanoidRootPart")
                            if hrp then hrp.Anchored = true end
                        end
                    end
                end)
            end
        end
    end
end)

-- ==================== ANTI AFK ====================
LP.Idled:Connect(function()
    if C.AntiKick then
        local vu = game:GetService("VirtualUser")
        vu:CaptureController()
        vu:ClickButton2(Vector2.new())
    end
end)

-- ==================== NOTIFIKASI ====================
Fluent:Notify({
    Title = "RENXX HUB v3.0",
    Content = "Bypass Gamepass loaded! By DEEP & RENXX",
    Duration = 5
})

print("RENXX HUB - Bypass Gamepass v3.0 (Fluent) Loaded!")
print("By DEEP & RENXX")
