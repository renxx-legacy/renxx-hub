--[[
    [By-pass] Bypass Gamepass V6.0 (Rayfield Edition)
    By: DEEP & RENXX
    Library: Rayfield (Sirius)
    Update: FIXED Speed Hack + Dropdown Player!
]]

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- ==================== SERVICES ====================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local MarketplaceService = game:GetService("MarketplaceService")
local TeleportService = game:GetService("TeleportService")
local RS = game:GetService("ReplicatedStorage")
local LP = Players.LocalPlayer
local Cam = workspace.CurrentCamera
local VU = game:GetService("VirtualUser")

-- ==================== CONFIG ====================
local C = {
    Bypass=false, BypassV2=false, SpeedBypass=false,
    AntiBan=false, AntiDetect=false, AntiKick=false, AntiAFK=false,
    BoostFPS=false, ShowFPS=false, LowPing=false,
    HighlightESP=false, TeamColor=Color3.fromRGB(0,255,0), EnemyColor=Color3.fromRGB(255,0,0), BotColor=Color3.fromRGB(255,255,0),
    Fullbright=false, NoFog=false, RemoveParticle=false,
    GodMode=false, SpeedHack=false, SpeedValue=50, JumpHack=false, JumpValue=50,
    InfJump=false, BunnyHop=false, NoClip=false, Fly=false, FlySpeed=50,
    SelectedPlayer=nil, FreezeBot=false,
}

-- ==================== MODULE HOOK ====================
local hookedModules, hookedRemotes, hookedValues = {}, {}, {}

task.spawn(function()
    task.wait(2)
    for _, obj in pairs(game:GetDescendants()) do
        pcall(function()
            if obj:IsA("ModuleScript") then
                local n = obj.Name:lower()
                if n:find("gamepass") or n:find("market") or n:find("purchase") or n:find("premium") then
                    local ok, data = pcall(require, obj)
                    if ok and type(data) == "table" then hookedModules[obj] = data end
                end
            elseif obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
                local n = obj.Name:lower()
                if n:find("gamepass") or n:find("purchase") or n:find("buy") then hookedRemotes[obj] = obj end
            elseif obj:IsA("ValueBase") then
                local n = obj.Name:lower()
                if n:find("gamepass") or n:find("owned") then hookedValues[obj] = obj end
            end
        end)
    end
    print("[RENXX] Hooked: " .. #hookedModules .. " modules")
end)

-- ==================== RAYFIELD WINDOW ====================
local Window = Rayfield:CreateWindow({
    Name = "[By-pass] V6.0 | RENXX",
    LoadingTitle = "RENXX HUB Loading...",
    LoadingSubtitle = "By DEEP & RENXX",
    ConfigurationSaving = {
        Enabled = false,
    },
    KeySystem = false,
})

-- ==================== TABS ====================
local ProtectionTab = Window:CreateTab("Protection", 4483362458)
local GamepassTab = Window:CreateTab("Gamepass", 4483362458)
local VisualTab = Window:CreateTab("Visual", 4483362458)
local PlayerTab = Window:CreateTab("Player", 4483362458)
local GameTab = Window:CreateTab("Game", 4483362458)

-- ==================== PROTECTION ====================
ProtectionTab:CreateSection("Bypass")

ProtectionTab:CreateButton({
    Name = "Bypass (Anti-Kick)",
    Callback = function()
        pcall(function()
            local mt = getrawmetatable(game)
            setreadonly(mt, false)
            local old = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                if getnamecallmethod() == "Kick" and self == LP then return nil end
                return old(self, ...)
            end)
            setreadonly(mt, true)
        end)
        Rayfield:Notify({Title="Bypass", Content="Anti-Kick aktif!", Duration=3})
    end,
})

ProtectionTab:CreateButton({
    Name = "Bypass V2",
    Callback = function()
        pcall(function()
            local mt = getrawmetatable(game)
            setreadonly(mt, false)
            local old = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                local m = getnamecallmethod()
                local a = {...}
                if m == "Kick" and self == LP then return nil end
                if m == "FireServer" then
                    local rn = self.Name:lower()
                    if rn:find("detect") or rn:find("ban") or rn:find("kick") then return nil end
                end
                return old(self, unpack(a))
            end)
            setreadonly(mt, true)
        end)
        Rayfield:Notify({Title="Bypass V2", Content="Aktif!", Duration=3})
    end,
})

ProtectionTab:CreateButton({
    Name = "Speed Bypass",
    Callback = function()
        pcall(function()
            local mt = getrawmetatable(game)
            setreadonly(mt, false)
            local old = mt.__index
            mt.__index = newcclosure(function(self, key)
                if key == "WalkSpeed" and self:IsA("Humanoid") and self.Parent == LP.Character then
                    return C.SpeedValue
                end
                return old(self, key)
            end)
            setreadonly(mt, true)
        end)
        Rayfield:Notify({Title="Speed Bypass", Content="Aktif!", Duration=3})
    end,
})

ProtectionTab:CreateToggle({
    Name = "Anti Ban",
    CurrentValue = false,
    Callback = function(v)
        C.AntiBan = v
        if v then
            pcall(function()
                local mt = getrawmetatable(game)
                setreadonly(mt, false)
                local old = mt.__namecall
                mt.__namecall = newcclosure(function(self, ...)
                    local m = getnamecallmethod()
                    if (m == "Kick" or m == "Ban") and self == LP then return nil end
                    return old(self, ...)
                end)
                setreadonly(mt, true)
            end)
        end
    end,
})

ProtectionTab:CreateToggle({
    Name = "Anti Detect",
    CurrentValue = false,
    Callback = function(v)
        C.AntiDetect = v
        if v then
            pcall(function()
                local mt = getrawmetatable(game)
                setreadonly(mt, false)
                local old = mt.__namecall
                mt.__namecall = newcclosure(function(self, ...)
                    local m = getnamecallmethod()
                    local a = {...}
                    if m == "FireServer" then
                        local rn = self.Name:lower()
                        if rn:find("detect") or rn:find("flag") then return nil end
                    end
                    return old(self, unpack(a))
                end)
                setreadonly(mt, true)
            end)
        end
    end,
})

ProtectionTab:CreateToggle({Name="Anti Kick", CurrentValue=false, Callback=function(v) C.AntiKick = v end})
ProtectionTab:CreateToggle({Name="Anti AFK", CurrentValue=false, Callback=function(v) C.AntiAFK = v end})

ProtectionTab:CreateSection("Performance")

ProtectionTab:CreateToggle({
    Name = "Boost FPS",
    CurrentValue = false,
    Callback = function(v)
        C.BoostFPS = v
        if v then
            for _, obj in pairs(workspace:GetDescendants()) do
                pcall(function()
                    if obj:IsA("ParticleEmitter") or obj:IsA("Trail") then obj.Enabled = false end
                end)
            end
        end
    end,
})

ProtectionTab:CreateDropdown({
    Name = "Set FPS Limit",
    Options = {"30","60","90","120","144"},
    CurrentOption = "60",
    Callback = function(o)
        pcall(function() setfpscap(tonumber(o)) end)
    end,
})

ProtectionTab:CreateToggle({Name="Show FPS", CurrentValue=false, Callback=function(v) C.ShowFPS = v end})

ProtectionTab:CreateToggle({
    Name = "Low Ping",
    CurrentValue = false,
    Callback = function(v)
        C.LowPing = v
        pcall(function() settings().Network.IncomingReplicationLag = v and -1 or 0 end)
    end,
})

-- ==================== GAMEPASS ====================
GamepassTab:CreateSection("Item & Purchase")

GamepassTab:CreateButton({
    Name = "Get All Item",
    Callback = function()
        for _, obj in pairs(RS:GetDescendants()) do
            pcall(function()
                if obj:IsA("RemoteEvent") then
                    local n = obj.Name:lower()
                    if n:find("item") or n:find("get") or n:find("shop") then obj:FireServer() end
                end
            end)
        end
    end,
})

GamepassTab:CreateButton({
    Name = "Free Purchase",
    Callback = function()
        local ok, prods = pcall(function() return MarketplaceService:GetDeveloperProductsAsync(game.PlaceId) end)
        if ok then
            for _, p in pairs(prods) do
                pcall(function()
                    MarketplaceService:PromptProductPurchase(LP, p.ProductId)
                    task.wait(0.2)
                    MarketplaceService:SignalPromptProductPurchaseFinished(LP.UserId, p.ProductId, true)
                end)
            end
        end
        pcall(function()
            local mt = getrawmetatable(game)
            setreadonly(mt, false)
            local old = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                local m = getnamecallmethod()
                if m == "ProcessReceipt" then return Enum.ProductPurchaseDecision.PurchaseGranted end
                if m == "PromptProductPurchase" then return true end
                return old(self, ...)
            end)
            setreadonly(mt, true)
        end)
    end,
})

GamepassTab:CreateButton({
    Name = "Free Purchase V2",
    Callback = function()
        pcall(function()
            local mt = getrawmetatable(game)
            setreadonly(mt, false)
            local old = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                local m = getnamecallmethod()
                if m == "PromptProductPurchase" or m == "PromptGamePassPurchase" then return true end
                return old(self, ...)
            end)
            setreadonly(mt, true)
        end)
    end,
})

GamepassTab:CreateSection("Free Gamepass")

GamepassTab:CreateButton({
    Name = "Free Gamepass V1",
    Callback = function()
        local ok, passes = pcall(function() return MarketplaceService:GetGamePassesAsync(game.PlaceId) end)
        if ok then
            for _, p in pairs(passes) do
                pcall(function()
                    MarketplaceService:PromptGamePassPurchase(LP, p.ProductId)
                    task.wait(0.3)
                    MarketplaceService:SignalPromptGamePassPurchaseFinished(LP, p.ProductId, true)
                end)
            end
        end
        for mod, data in pairs(hookedModules) do
            pcall(function()
                for k, v in pairs(data) do
                    local kn = tostring(k):lower()
                    if kn:find("gamepass") or kn:find("owned") or kn:find("premium") then
                        if type(v) == "boolean" then data[k] = true end
                        if type(v) == "number" then data[k] = 1 end
                        if type(v) == "function" then data[k] = function() return true end end
                    end
                end
            end)
        end
    end,
})

GamepassTab:CreateButton({
    Name = "Free Gamepass V2",
    Callback = function()
        pcall(function()
            local mt = getrawmetatable(game)
            setreadonly(mt, false)
            local old = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                local m = getnamecallmethod()
                if m == "UserOwnsGamePassAsync" then return true end
                if m == "PlayerOwnsAsset" then return true end
                return old(self, ...)
            end)
            setreadonly(mt, true)
        end)
    end,
})

GamepassTab:CreateButton({
    Name = "Free Gamepass V3",
    Callback = function()
        for mod, data in pairs(hookedModules) do
            pcall(function()
                for k, v in pairs(data) do
                    local kn = tostring(k):lower()
                    if kn:find("gamepass") or kn:find("owned") or kn:find("premium") or kn:find("vip") then
                        if type(v) == "boolean" then data[k] = true end
                        if type(v) == "number" then data[k] = 1 end
                        if type(v) == "string" then data[k] = "owned" end
                        if type(v) == "function" then data[k] = function() return true end end
                    end
                end
            end)
        end
        for valObj in pairs(hookedValues) do
            pcall(function()
                if valObj:IsA("BoolValue") then valObj.Value = true end
                if valObj:IsA("NumberValue") or valObj:IsA("IntValue") then valObj.Value = 9999 end
            end)
        end
    end,
})

-- ==================== VISUAL ====================
VisualTab:CreateSection("Highlight ESP")

VisualTab:CreateToggle({
    Name = "ESP Highlight",
    CurrentValue = false,
    Callback = function(v)
        C.HighlightESP = v
        if not v then
            for _, p in pairs(Players:GetPlayers()) do
                if p.Character and p.Character:FindFirstChild("RENXX_HL") then
                    p.Character.RENXX_HL:Destroy()
                end
            end
        end
    end,
})

VisualTab:CreateColorPicker({
    Name = "Team Highlight Color",
    Color = Color3.fromRGB(0,255,0),
    Callback = function(c) C.TeamColor = c end,
})

VisualTab:CreateColorPicker({
    Name = "Enemy Highlight Color",
    Color = Color3.fromRGB(255,0,0),
    Callback = function(c) C.EnemyColor = c end,
})

VisualTab:CreateColorPicker({
    Name = "BOT/NPC Highlight Color",
    Color = Color3.fromRGB(255,255,0),
    Callback = function(c) C.BotColor = c end,
})

VisualTab:CreateSection("Lighting")

VisualTab:CreateToggle({
    Name = "Fullbright",
    CurrentValue = false,
    Callback = function(v)
        C.Fullbright = v
        Lighting.Brightness = v and 2 or 1
        Lighting.ClockTime = v and 12 or 14
        Lighting.GlobalShadows = not v
    end,
})

VisualTab:CreateToggle({
    Name = "No FOG",
    CurrentValue = false,
    Callback = function(v)
        C.NoFog = v
        Lighting.FogEnd = v and 100000 or 1000
    end,
})

VisualTab:CreateToggle({
    Name = "Remove Particle",
    CurrentValue = false,
    Callback = function(v)
        C.RemoveParticle = v
        if v then
            for _, obj in pairs(workspace:GetDescendants()) do
                pcall(function()
                    if obj:IsA("ParticleEmitter") or obj:IsA("Trail") then obj.Enabled = false end
                end)
            end
        end
    end,
})

-- ==================== PLAYER ====================
PlayerTab:CreateSection("Health")
PlayerTab:CreateToggle({Name="God Mode", CurrentValue=false, Callback=function(v) C.GodMode = v end})

PlayerTab:CreateSection("Movement")

PlayerTab:CreateToggle({
    Name = "Speed Hack",
    CurrentValue = false,
    Callback = function(v)
        C.SpeedHack = v
        if not v and LP.Character then
            local h = LP.Character:FindFirstChildOfClass("Humanoid")
            if h then h.WalkSpeed = 16 end
        end
    end,
})

PlayerTab:CreateSlider({
    Name = "Set Speed",
    Range = {0, 100},
    Increment = 1,
    Suffix = "x",
    CurrentValue = 50,
    Callback = function(v) C.SpeedValue = v end,
})

PlayerTab:CreateToggle({
    Name = "Jump Hack",
    CurrentValue = false,
    Callback = function(v)
        C.JumpHack = v
        if not v and LP.Character then
            local h = LP.Character:FindFirstChildOfClass("Humanoid")
            if h then h.UseJumpPower = true; h.JumpPower = 50 end
        end
    end,
})

PlayerTab:CreateSlider({
    Name = "Set Jump",
    Range = {0, 100},
    Increment = 1,
    Suffix = "x",
    CurrentValue = 50,
    Callback = function(v) C.JumpValue = v end,
})

PlayerTab:CreateToggle({Name="Infinity Jump", CurrentValue=false, Callback=function(v) C.InfJump = v end})
PlayerTab:CreateToggle({Name="Bunny Hop", CurrentValue=false, Callback=function(v) C.BunnyHop = v end})
PlayerTab:CreateToggle({Name="No Clip", CurrentValue=false, Callback=function(v) C.NoClip = v end})

PlayerTab:CreateSection("Fly")
PlayerTab:CreateToggle({Name="Fly", CurrentValue=false, Callback=function(v) C.Fly = v end})
PlayerTab:CreateSlider({
    Name = "Fly Speed",
    Range = {10, 200},
    Increment = 1,
    Suffix = "x",
    CurrentValue = 50,
    Callback = function(v) C.FlySpeed = v end,
})

PlayerTab:CreateSection("Other")
PlayerTab:CreateButton({
    Name = "Reset Character",
    Callback = function()
        if LP.Character then
            local h = LP.Character:FindFirstChildOfClass("Humanoid")
            if h then h.Health = 0 end
        end
    end,
})

-- ==================== GAME ====================
GameTab:CreateSection("Player")

-- FIX: Dropdown Player yang bisa auto-update
local function GetPlayerList()
    local list = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP then table.insert(list, p.Name) end
    end
    if #list == 0 then list = {"(No Player)"} end
    return list
end

local PlayerDropdown = GameTab:CreateDropdown({
    Name = "Select Player",
    Options = GetPlayerList(),
    CurrentOption = {"(Pilih Player)"},
    Callback = function(option)
        if type(option) == "table" then
            C.SelectedPlayer = option[1]
        else
            C.SelectedPlayer = option
        end
    end,
})

-- Auto-update dropdown tiap 3 detik
task.spawn(function()
    while task.wait(3) do
        pcall(function()
            local list = GetPlayerList()
            PlayerDropdown:Refresh(list, true)
        end)
    end
end)

GameTab:CreateButton({
    Name = "Teleport To Player",
    Callback = function()
        if C.SelectedPlayer and C.SelectedPlayer ~= "(Pilih Player)" and C.SelectedPlayer ~= "(No Player)" then
            local target = Players:FindFirstChild(C.SelectedPlayer)
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") and LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
                LP.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -5)
                Rayfield:Notify({Title="Teleport", Content="Ke " .. C.SelectedPlayer, Duration=3})
            else
                Rayfield:Notify({Title="Teleport", Content="Target tidak ditemukan", Duration=3})
            end
        else
            Rayfield:Notify({Title="Teleport", Content="Pilih player dulu!", Duration=3})
        end
    end,
})

GameTab:CreateToggle({Name="Freeze BOT/NPC", CurrentValue=false, Callback=function(v) C.FreezeBot = v end})

GameTab:CreateSection("Server")
GameTab:CreateButton({
    Name = "Rejoin Server",
    Callback = function() TeleportService:Teleport(game.PlaceId, LP) end,
})

-- ==================== MAIN LOOP (SPEED HACK FIXED) ====================
RunService.Heartbeat:Connect(function()
    if not LP.Character then return end
    local h = LP.Character:FindFirstChildOfClass("Humanoid")
    local hrp = LP.Character:FindFirstChild("HumanoidRootPart")
    if not h then return end

    -- GOD MODE
    if C.GodMode then h.Health = h.MaxHealth end

    -- SPEED HACK (FORCE LOOP - anti reset)
    if C.SpeedHack then
        if h.WalkSpeed ~= C.SpeedValue then
            h.WalkSpeed = C.SpeedValue
        end
        -- Pakai BodyVelocity kalau game nge-reset WalkSpeed
        if hrp then
            local bv = hrp:FindFirstChild("RENXX_SpeedBV")
            if not bv then
                bv = Instance.new("BodyVelocity")
                bv.Name = "RENXX_SpeedBV"
                bv.MaxForce = Vector3.new(math.huge, 0, math.huge)
                bv.Velocity = Vector3.new(0,0,0)
                bv.Parent = hrp
            end
            if h.MoveDirection.Magnitude > 0 then
                bv.Velocity = h.MoveDirection * C.SpeedValue
            else
                bv.Velocity = Vector3.new(0,0,0)
            end
        end
    else
        if hrp then
            local bv = hrp:FindFirstChild("RENXX_SpeedBV")
            if bv then bv:Destroy() end
        end
    end

    -- JUMP HACK
    if C.JumpHack then
        h.UseJumpPower = true
        if h.JumpPower ~= C.JumpValue then h.JumpPower = C.JumpValue end
    end

    -- BUNNY HOP
    if C.BunnyHop and h:GetState() == Enum.HumanoidStateType.Landed then
        h:ChangeState(Enum.HumanoidStateType.Jumping)
    end

    -- NO CLIP
    if C.NoClip then
        for _, p in pairs(LP.Character:GetDescendants()) do
            if p:IsA("BasePart") and p.CanCollide then p.CanCollide = false end
        end
    end

    -- FLY
    if C.Fly and hrp then
        if not hrp:FindFirstChild("RENXX_FlyV") then
            local bv = Instance.new("BodyVelocity")
            bv.Name = "RENXX_FlyV"
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bv.Parent = hrp
        end
        local md = Vector3.new()
        if UIS:IsKeyDown(Enum.KeyCode.W) then md = md + Cam.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.S) then md = md - Cam.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.A) then md = md - Cam.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.D) then md = md + Cam.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.Space) then md = md + Vector3.new(0,1,0) end
        if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then md = md - Vector3.new(0,1,0) end
        hrp.RENXX_FlyV.Velocity = md * C.FlySpeed
    else
        if hrp then
            local bv = hrp:FindFirstChild("RENXX_FlyV")
            if bv then bv:Destroy() end
        end
    end
end)

-- ==================== SECONDARY LOOP ====================
task.spawn(function()
    while task.wait(0.5) do
        if C.HighlightESP then
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LP and p.Character then
                    local hl = p.Character:FindFirstChild("RENXX_HL")
                    if not hl then
                        hl = Instance.new("Highlight")
                        hl.Name = "RENXX_HL"
                        hl.Parent = p.Character
                    end
                    local sameTeam = p.Team and LP.Team and p.Team == LP.Team
                    hl.FillColor = sameTeam and C.TeamColor or C.EnemyColor
                    hl.OutlineColor = Color3.fromRGB(255,255,255)
                end
            end
            for _, obj in pairs(workspace:GetChildren()) do
                pcall(function()
                    if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") and not Players:GetPlayerFromCharacter(obj) then
                        local hl = obj:FindFirstChild("RENXX_BOT_HL")
                        if not hl then
                            hl = Instance.new("Highlight")
                            hl.Name = "RENXX_BOT_HL"
                            hl.Parent = obj
                        end
                        hl.FillColor = C.BotColor
                    end
                end)
            end
        end

        if C.FreezeBot then
            for _, obj in pairs(workspace:GetChildren()) do
                pcall(function()
                    if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") and not Players:GetPlayerFromCharacter(obj) then
                        local h = obj:FindFirstChildOfClass("Humanoid")
                        if h then h.WalkSpeed = 0; h.JumpPower = 0 end
                    end
                end)
            end
        end

        if C.AntiAFK then
            VU:CaptureController()
            VU:ClickButton2(Vector2.new())
        end
    end
end)

-- ==================== INFINITE JUMP ====================
local function setupInfJump(char)
    local h = char:WaitForChild("Humanoid")
    h.StateChanged:Connect(function(_, newState)
        if C.InfJump and newState == Enum.HumanoidStateType.Freefall then
            task.wait(0.1)
            if C.InfJump then h:ChangeState(Enum.HumanoidStateType.Jumping) end
        end
    end)
end
if LP.Character then setupInfJump(LP.Character) end
LP.CharacterAdded:Connect(setupInfJump)

-- ==================== NOTIFIKASI ====================
Rayfield:Notify({
    Title = "RENXX HUB V6.0",
    Content = "Speed Hack & Dropdown Fixed!",
    Duration = 5,
})

print("[By-pass] V6.0 Rayfield - Loaded!")
