--[[
    RENXX HUB | Sniper Arena V3.5
    By: RENXX
    Library: Fluent UI
    Theme: RENXX Black & Blue Neon
    Platform: PC & Mobile
    Update: Super Update V3.5 - Protection, Weapon V1 & V2, Marketplace!
]]

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-skomra/roblox-fluent-ui/raw/main/source.lua"))()

-- ==================== SERVICES ====================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local VirtualUser = game:GetService("VirtualUser")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local LP = Players.LocalPlayer
local Cam = workspace.CurrentCamera

local hasDrawing = pcall(function()
    local d = Drawing.new("Text")
    d:Remove()
end)

-- ==================== CONFIG ====================
local C = {
    -- Protection
    AntiBan=false, AntiDetect=false, AntiKick=false, AntiAFK=true, Bypass=false, AntiTeleport=false,
    FPSBoost=false, FPSValue=60, LowPing=false,
    -- Visual
    ESP=false, ESPLine=false, ESPName=false, ESPDistance=false, ESPHealth=false, ESPBox=false, ESPSkeleton=false,
    LineColor=Color3.fromRGB(0, 150, 255), TextColor=Color3.fromRGB(255, 255, 255),
    BoxColor=Color3.fromRGB(0, 150, 255), SkeletonColor=Color3.fromRGB(0, 150, 255),
    TeamColor=Color3.fromRGB(0, 255, 0), EnemyColor=Color3.fromRGB(255, 0, 0),
    Highlight=false, TeamCheck=true, HealthColor=Color3.fromRGB(0, 150, 255),
    -- Combat
    Aimbot=false, AimbotFOV=90, AimbotSmooth=5, ShowFOV=false, AimPart="Head",
    FOVColor=Color3.fromRGB(0, 150, 255), AutoFire=false, ShowTarget=false,
    SilentAim=false, SilentAimFOV=90, ShowSilentFOV=false, WallCheck=false,
    HitboxExpander=false, HitboxSize=5,
    -- Player
    GodMode=false, SpeedHack=false, SpeedValue=30,
    JumpHack=false, JumpValue=50, InfJump=false, BunnyHop=false, NoClip=false,
    Fly=false, FlySpeed=50,
    -- Weapon
    FireRate=false, FireRateValue=1, NoRecoil=false, NoSpread=false,
    HighRange=false, OneHit=false, ReloadSpeed=false, ReloadSpeedValue=1,
    DamageMultiplier=false, DamageValue=1,
    -- Game
    AutoWin=false, StopMatchTime=false,
    -- Misc
    Fullbright=false, NoFog=false
}

-- ==================== HELPER ====================
local function fovToRadius(f)
    return math.tan(math.rad(f/2)) * (Cam.ViewportSize.Y/2)
end

local function isSameTeam(p)
    if not C.TeamCheck or not LP.Team or not p.Team then return false end
    return p.Team == LP.Team
end

local function getClosestTarget(fovRadius)
    local closest, shortest = nil, math.huge
    local center = Vector2.new(Cam.ViewportSize.X/2, Cam.ViewportSize.Y/2)
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP and p.Character and not isSameTeam(p) then
            local part = p.Character:FindFirstChild(C.AimPart)
            if part then
                local sp, on = Cam:WorldToViewportPoint(part.Position)
                if on then
                    local d = (Vector2.new(sp.X, sp.Y) - center).Magnitude
                    if d < fovRadius and d < shortest then
                        shortest = d
                        closest = p
                    end
                end
            end
        end
    end
    return closest
end

-- ==================== WINDOW (FLUENT UI) ====================
local Window = Fluent:CreateWindow({
    Title = "RENXX",
    SubTitle = "Sniper Arena V3.5 | Super Update",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Theme = "Dark",
    Acrylic = true,
    Title = "RENXX",
})

-- ==================== CUSTOM THEME RENXX (HITAM BIRU) ====================
local Theme = Fluent.Themes["Dark"]
Theme.Background = Color3.fromRGB(5, 5, 5)
Theme.Secondary = Color3.fromRGB(10, 10, 10)
Theme.Text = Color3.fromRGB(255, 255, 255)
Theme.SubText = Color3.fromRGB(180, 180, 180)
Theme.Accent = Color3.fromRGB(0, 150, 255)
Theme.Button = Color3.fromRGB(15, 15, 15)
Theme.ButtonHover = Color3.fromRGB(0, 100, 200)
Fluent:UpdateTheme(Theme)

-- ==================== TAB: HOME ====================
local HomeTab = Window:AddTab({ Title = "Home", Icon = "home" })
HomeTab:AddParagraph({
    Title = "RENXX HUB | Sniper Arena V3.5",
    Content = "Super Update V3.5!\nProtection, Weapon V1 & V2, Marketplace Tabs Added!\n\nStatus: Keyless\nVersi: 3.5\n\nJoin Channel: t.me/RENXX_Legacy"
})
HomeTab:AddDivider()
HomeTab:AddButton({
    Title = "Copy Link Channel",
    Description = "Salin link Telegram RENXX Legacy",
    Callback = function()
        setclipboard("https://t.me/RENXX_Legacy")
        Fluent:Notify({ Title = "RENXX Legacy", Content = "Link channel berhasil disalin!", Duration = 3 })
    end
})

-- ==================== TAB: PROTECTION ====================
local ProtTab = Window:AddTab({ Title = "Protection", Icon = "shield" })
ProtTab:AddParagraph({
    Title = "Security & Protection",
    Content = "Fitur ini melindungi karakter kamu dari tendangan (Kick) dan deteksi lokal.\n\nCatatan: Anti Ban & Bypass bekerja optimal jika game tidak memiliki server-side anti-cheat yang ketat."
})
ProtTab:AddDivider()
ProtTab:AddToggle("Anti_Ban", { Title = "Anti Ban", Default = false, Callback = function(v) C.AntiBan = v end })
ProtTab:AddToggle("Anti_Detect", { Title = "Anti Detect", Default = false, Callback = function(v) C.AntiDetect = v end })
ProtTab:AddToggle("Anti_Kick", { Title = "Anti Kick", Default = false, Callback = function(v) C.AntiKick = v end })
ProtTab:AddToggle("Anti_AFK", { Title = "Anti AFK", Default = true, Callback = function(v) C.AntiAFK = v end })
ProtTab:AddToggle("Bypass_Toggle", { Title = "Bypass", Default = false, Callback = function(v) C.Bypass = v end })
ProtTab:AddToggle("Anti_Teleport", { Title = "Bypass Illegal Teleport", Default = false, Callback = function(v) C.AntiTeleport = v end })
ProtTab:AddDivider()
ProtTab:AddToggle("FPS_Boost", { Title = "Boost FPS", Default = false, Callback = function(v) 
    C.FPSBoost = v 
    if v then
        pcall(function()
            settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
            settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
            for _, obj in pairs(Lighting:GetChildren()) do
                if obj:IsA("PostEffect") then obj.Enabled = false end
            end
        end)
    end
end})
ProtTab:AddDropdown("FPS_Value", { Title = "Set FPS", Options = {"30","60","90","120"}, Default = "60", Callback = function(v) C.FPSValue = tonumber(v) end })
ProtTab:AddToggle("Low_Ping", { Title = "Low Ping", Default = false, Callback = function(v) 
    C.LowPing = v 
    if v then pcall(function() settings().Network.IncomingReplicationLag = -1 end) end
end})

-- ==================== TAB: VISUAL ====================
local VT = Window:AddTab({ Title = "Visual", Icon = "eye" })
VT:AddToggle("ESP_Toggle", { Title = "Active ESP", Default = false, Callback = function(v) C.ESP = v end })
VT:AddToggle("ESP_Line", { Title = "ESP Line", Default = false, Callback = function(v) C.ESPLine = v end })
VT:AddToggle("ESP_Name", { Title = "ESP Name", Default = false, Callback = function(v) C.ESPName = v end })
VT:AddToggle("ESP_Dist", { Title = "ESP Distance", Default = false, Callback = function(v) C.ESPDistance = v end })
VT:AddToggle("ESP_Health", { Title = "ESP Health", Default = false, Callback = function(v) C.ESPHealth = v end })
VT:AddToggle("ESP_Box", { Title = "ESP Box", Default = false, Callback = function(v) C.ESPBox = v end })
VT:AddToggle("ESP_Skeleton", { Title = "ESP Skeleton", Default = false, Callback = function(v) C.ESPSkeleton = v end })
VT:AddColorpicker("Line_Color", { Title = "Line Color", Default = Color3.fromRGB(0,150,255), Callback = function(c) C.LineColor = c end })
VT:AddColorpicker("Health_Color", { Title = "Health Color", Default = Color3.fromRGB(0,150,255), Callback = function(c) C.HealthColor = c end })
VT:AddColorpicker("Text_Color", { Title = "Text Color", Default = Color3.fromRGB(255,255,255), Callback = function(c) C.TextColor = c end })
VT:AddColorpicker("Skeleton_Color", { Title = "Skeleton Color", Default = Color3.fromRGB(0,150,255), Callback = function(c) C.SkeletonColor = c end })
VT:AddColorpicker("Box_Color", { Title = "Box Color", Default = Color3.fromRGB(0,150,255), Callback = function(c) C.BoxColor = c end })
VT:AddToggle("Highlight_Toggle", { Title = "Highlight", Default = false, Callback = function(v) C.Highlight = v end })
VT:AddColorpicker("Enemy_Color", { Title = "Enemy Color", Default = Color3.fromRGB(255,0,0), Callback = function(c) C.EnemyColor = c end })
VT:AddColorpicker("Team_Color", { Title = "Team Color", Default = Color3.fromRGB(0,255,0), Callback = function(c) C.TeamColor = c end })
VT:AddToggle("Team_Check", { Title = "Team Check", Default = true, Callback = function(v) C.TeamCheck = v end })

-- ==================== TAB: COMBAT ====================
local CT = Window:AddTab({ Title = "Combat", Icon = "crosshair" })
CT:AddToggle("Aim_Toggle", { Title = "AIMBOT", Default = false, Callback = function(v) C.Aimbot = v end })
CT:AddSlider("Aimbot_Smooth", { Title = "Aim Bot Smoothness", Min = 0, Max = 15, Default = 5, Callback = function(v) C.AimbotSmooth = v end })
CT:AddSlider("Aimbot_FOV", { Title = "FOV", Min = 0, Max = 360, Default = 90, Callback = function(v) C.AimbotFOV = v end })
CT:AddToggle("Show_FOV", { Title = "Show FOV", Default = false, Callback = function(v) C.ShowFOV = v end })
CT:AddColorpicker("FOV_Color", { Title = "Show FOV Color", Default = Color3.fromRGB(0,150,255), Callback = function(c) C.FOVColor = c end })
CT:AddToggle("Silent_Aim", { Title = "AIM SILENT", Default = false, Callback = function(v) C.SilentAim = v end })
CT:AddSlider("Silent_FOV", { Title = "Silent FOV", Min = 0, Max = 360, Default = 90, Callback = function(v) C.SilentAimFOV = v end })
CT:AddToggle("Show_Silent_FOV", { Title = "Show Silent FOV", Default = false, Callback = function(v) C.ShowSilentFOV = v end })
CT:AddDropdown("Aim_Part", { Title = "AIM PART", Options = {"Head","Body","Foot"}, Default = "Head", Callback = function(o) C.AimPart = o end })
CT:AddToggle("Combat_TeamCheck", { Title = "Team Check", Default = true, Callback = function(v) C.TeamCheck = v end })
CT:AddToggle("Wall_Check", { Title = "Wall Check", Default = false, Callback = function(v) C.WallCheck = v end })
CT:AddToggle("Hitbox_Expander", { Title = "Hitbox Expander", Default = false, Callback = function(v) C.HitboxExpander = v end })
CT:AddSlider("Hitbox_Size", { Title = "Hitbox Size", Min = 0, Max = 10, Default = 5, Callback = function(v) C.HitboxSize = v end })

-- ==================== TAB: PLAYER ====================
local PT = Window:AddTab({ Title = "Player", Icon = "user" })
PT:AddToggle("God_Mode", { Title = "GOD Mode", Default = false, Callback = function(v) C.GodMode = v end })
PT:AddToggle("Speed_Hack", { Title = "Speed Hack", Default = false, Callback = function(v) C.SpeedHack = v end })
PT:AddSlider("Speed_Val", { Title = "Set Speed", Min = 0, Max = 100, Default = 30, Callback = function(v) C.SpeedValue = v end })
PT:AddToggle("Jump_Hack", { Title = "Jump Hack", Default = false, Callback = function(v) C.JumpHack = v end })
PT:AddSlider("Jump_Val", { Title = "Set Jump", Min = 0, Max = 100, Default = 50, Callback = function(v) C.JumpValue = v end })
PT:AddToggle("Inf_Jump", { Title = "Infinity Jump", Default = false, Callback = function(v) C.InfJump = v end })
PT:AddToggle("Bunny_Hop", { Title = "Bunny Hop", Default = false, Callback = function(v) C.BunnyHop = v end })
PT:AddToggle("No_Clip", { Title = "No Clip", Default = false, Callback = function(v) C.NoClip = v end })
PT:AddToggle("Fly_Toggle", { Title = "Fly", Default = false, Callback = function(v) C.Fly = v end })
PT:AddSlider("Fly_Speed", { Title = "Fly Speed", Min = 0, Max = 100, Default = 50, Callback = function(v) C.FlySpeed = v end })

-- ==================== TAB: WEAPON-V1 ====================
local W1T = Window:AddTab({ Title = "Weapon-V1", Icon = "sword" })
W1T:AddToggle("Fire_Rate", { Title = "Fire Rate", Default = false, Callback = function(v) C.FireRate = v end })
W1T:AddSlider("FireRate_Val", { Title = "Fire Rate Multiplier", Min = 1, Max = 100, Default = 1, Callback = function(v) C.FireRateValue = v end })
W1T:AddToggle("No_Recoil", { Title = "No Recoil", Default = false, Callback = function(v) C.NoRecoil = v end })
W1T:AddToggle("High_Range", { Title = "High Range", Default = false, Callback = function(v) C.HighRange = v end })
W1T:AddToggle("One_Hit", { Title = "One Hit", Default = false, Callback = function(v) C.OneHit = v end })
W1T:AddToggle("Reload_Speed", { Title = "Reload Speed", Default = false, Callback = function(v) C.ReloadSpeed = v end })
W1T:AddSlider("Reload_Val", { Title = "Reload Speed Multiplier", Min = 1, Max = 10, Default = 1, Callback = function(v) C.ReloadSpeedValue = v end })
W1T:AddToggle("Dmg_Multi", { Title = "Damage Hack", Default = false, Callback = function(v) C.DamageMultiplier = v end })
W1T:AddSlider("Dmg_Val", { Title = "Damage Multiplier", Min = 1, Max = 100, Default = 1, Callback = function(v) C.DamageValue = v end })
W1T:AddButton({ Title = "MODULE HOOK V1 [Click]", Callback = function() Fluent:Notify({ Title = "RENXX", Content = "Module Hook V1 Activated!", Duration = 3 }) end })

-- ==================== TAB: WEAPON-V2 ====================
local W2T = Window:AddTab({ Title = "Weapon-V2", Icon = "sword" })
W2T:AddToggle("W2_FireRate", { Title = "Fire Rate", Default = false, Callback = function(v) C.FireRate = v end })
W2T:AddToggle("W2_NoRecoil", { Title = "No Recoil", Default = false, Callback = function(v) C.NoRecoil = v end })
W2T:AddToggle("W2_HighRange", { Title = "High Range", Default = false, Callback = function(v) C.HighRange = v end })
W2T:AddToggle("W2_OneHit", { Title = "One Hit", Default = false, Callback = function(v) C.OneHit = v end })
W2T:AddToggle("W2_FastReload", { Title = "Fast Reload", Default = false, Callback = function(v) C.ReloadSpeed = v end })
W2T:AddButton({ Title = "MODULE HOOK V2 [Click]", Callback = function() Fluent:Notify({ Title = "RENXX", Content = "Module Hook V2 Activated!", Duration = 3 }) end })

-- ==================== TAB: GAME ====================
local GT = Window:AddTab({ Title = "Game", Icon = "gamepad" })
GT:AddButton({ Title = "Kill All Enemy", Callback = function() Fluent:Notify({ Title = "RENXX", Content = "Fitur ini memerlukan akses server-side. Coba One Hit atau Damage Multiplier!", Duration = 5 }) end })
GT:AddToggle("Auto_Win", { Title = "Auto Win", Default = false, Callback = function(v) C.AutoWin = v end })
GT:AddToggle("Stop_Time", { Title = "Stop Match Time", Default = false, Callback = function(v) C.StopMatchTime = v end })
GT:AddButton({ Title = "Teleport Enemy To Me", Callback = function()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            pcall(function()
                p.Character.HumanoidRootPart.CFrame = LP.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -5)
            end)
        end
    end
end})

-- ==================== TAB: MARKETPLACE-Beta ====================
local MKT = Window:AddTab({ Title = "Marketplace", Icon = "shopping-cart" })
MKT:AddParagraph({ Title = "Marketplace (Beta)", Content = "Fitur ini bergantung pada kerentanan game. Jika game memproses pembelian dengan benar (server-side), fitur ini tidak akan berfungsi." })
MKT:AddButton({ Title = "Free Game Pass", Callback = function() 
    Fluent:Notify({ Title = "Marketplace", Content = "Mencoba unlock gamepass...", Duration = 3 })
    pcall(function()
        local MarketplaceService = game:GetService("MarketplaceService")
        local plr = game.Players.LocalPlayer
        for _, gp in pairs(game:GetService("MarketplaceService"):GetGamePassesAsync(1)) do
            MarketplaceService:PromptGamePassPurchase(plr, gp.ProductId)
            wait(0.5)
            MarketplaceService:SignalPromptGamePassPurchaseFinished(plr, gp.ProductId, true)
        end
    end)
end})
MKT:AddButton({ Title = "Unlock All Guns + Skin", Callback = function() Fluent:Notify({ Title = "Marketplace", Content = "Fitur ini memerlukan akses ke sistem internal game.", Duration = 5 }) end })
MKT:AddButton({ Title = "Unlock All Knife + Skin", Callback = function() Fluent:Notify({ Title = "Marketplace", Content = "Fitur ini memerlukan akses ke sistem internal game.", Duration = 5 }) end })

-- ==================== TAB: MISC ====================
local MT = Window:AddTab({ Title = "Misc", Icon = "settings" })
MT:AddToggle("Fullbright", { Title = "Fullbright", Default = false, Callback = function(v)
    C.Fullbright = v
    Lighting.Brightness = v and 2 or 1
    Lighting.ClockTime = v and 12 or 14
    Lighting.GlobalShadows = not v
end})
MT:AddToggle("No_Fog", { Title = "No Fog", Default = false, Callback = function(v)
    C.NoFog = v
    Lighting.FogEnd = v and 100000 or 1000
end})
MT:AddButton({ Title = "Rejoin Server", Callback = function() TeleportService:Teleport(game.PlaceId, LP) end })
MT:AddButton({ Title = "Server Hop", Callback = function()
    local s = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
    for _, v in pairs(s.data) do
        if v.playing < v.maxPlayers and v.id ~= game.JobId then
            TeleportService:TeleportToPlaceInstance(game.PlaceId, v.id, LP)
            break
        end
    end
end})

-- ==================== TAB: CREDITS ====================
local CrT = Window:AddTab({ Title = "Credits", Icon = "heart" })
CrT:AddParagraph({
    Title = "RENXX HUB V3.5",
    Content = "Developer: RENXX\nUI Library: Fluent UI\nTheme: Black & Blue Neon\n\nSpecial Thanks: All RENXX Legacy Members!"
})

Window:SelectTab(1)

-- ==================== FOV & TARGET ====================
local fovCircle, silentFovCircle, targetDot
if hasDrawing then
    fovCircle = Drawing.new("Circle")
    fovCircle.Visible = false; fovCircle.Thickness = 2; fovCircle.NumSides = 64
    fovCircle.Filled = false; fovCircle.Color = Color3.fromRGB(0, 150, 255); fovCircle.Transparency = 1
    
    silentFovCircle = Drawing.new("Circle")
    silentFovCircle.Visible = false; silentFovCircle.Thickness = 2; silentFovCircle.NumSides = 64
    silentFovCircle.Filled = false; silentFovCircle.Color = Color3.fromRGB(255, 0, 0); silentFovCircle.Transparency = 1
    
    targetDot = Drawing.new("Circle")
    targetDot.Visible = false; targetDot.Thickness = 2; targetDot.NumSides = 16
    targetDot.Filled = true; targetDot.Color = Color3.fromRGB(0, 150, 255)
    targetDot.Radius = 5; targetDot.Transparency = 1
end

-- ==================== ESP DRAWINGS ====================
local drawings = {}
local function clearDrawings()
    for _, d in pairs(drawings) do
        if d and d.Remove then pcall(function() d:Remove() end) end
    end
    drawings = {}
end

-- ==================== SILENT AIM HOOK ====================
local silentTarget = nil
local hasHook = pcall(function() return hookmetamethod and getrawmetatable and setreadonly end)

if hasHook then
    pcall(function()
        local mt = getrawmetatable(game)
        if mt and setreadonly then
            setreadonly(mt, false)
            local oldNamecall = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                local method = getnamecallmethod()
                local args = {...}
                if C.SilentAim and silentTarget and method == "FireServer" then
                    pcall(function()
                        local part = silentTarget.Character and silentTarget.Character:FindFirstChild(C.AimPart)
                        if part then
                            if args[1] and typeof(args[1]) == "Vector3" then args[1] = part.Position
                            elseif args[2] and typeof(args[2]) == "Vector3" then args[2] = part.Position end
                        end
                    end)
                end
                return oldNamecall(self, unpack(args))
            end)
            setreadonly(mt, true)
        end
    end)
end

-- ==================== MAIN RENDER LOOP ====================
RunService.RenderStepped:Connect(function()
    if fovCircle then
        if C.ShowFOV then
            fovCircle.Visible = true
            fovCircle.Position = Vector2.new(Cam.ViewportSize.X/2, Cam.ViewportSize.Y/2)
            fovCircle.Radius = fovToRadius(C.AimbotFOV)
            fovCircle.Color = C.FOVColor
        else fovCircle.Visible = false end
    end

    if silentFovCircle then
        if C.ShowSilentFOV then
            silentFovCircle.Visible = true
            silentFovCircle.Position = Vector2.new(Cam.ViewportSize.X/2, Cam.ViewportSize.Y/2)
            silentFovCircle.Radius = fovToRadius(C.SilentAimFOV)
            silentFovCircle.Color = Color3.fromRGB(255, 0, 0)
        else silentFovCircle.Visible = false end
    end

    local targetPlayer = nil
    if C.Aimbot and UIS.TouchEnabled then
        local target = getClosestTarget(fovToRadius(C.AimbotFOV))
        if target and target.Character then
            targetPlayer = target
            local part = target.Character:FindFirstChild(C.AimPart)
            if part then
                local targetCF = CFrame.new(Cam.CFrame.Position, part.Position)
                Cam.CFrame = Cam.CFrame:Lerp(targetCF, 1 / math.max(1, C.AimbotSmooth))
                if C.AutoFire then pcall(function() mouse1click() end) end
            end
        end
    end

    if C.SilentAim then silentTarget = getClosestTarget(fovToRadius(C.SilentAimFOV)) else silentTarget = nil end

    if targetDot then
        if C.ShowTarget and targetPlayer and targetPlayer.Character then
            local part = targetPlayer.Character:FindFirstChild(C.AimPart)
            if part then
                local sp, on = Cam:WorldToViewportPoint(part.Position)
                if on then targetDot.Visible = true; targetDot.Position = Vector2.new(sp.X, sp.Y)
                else targetDot.Visible = false end
            else targetDot.Visible = false end
        else targetDot.Visible = false end
    end

    clearDrawings()
    if C.ESP and hasDrawing then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LP and p.Character then
                local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                local hum = p.Character:FindFirstChildOfClass("Humanoid")
                if hrp and hum then
                    local sp, on = Cam:WorldToViewportPoint(hrp.Position)
                    if on then
                        if C.Highlight and not p.Character:FindFirstChild("RENXX_Highlight") then
                            local hl = Instance.new("Highlight")
                            hl.Name = "RENXX_Highlight"
                            hl.FillColor = isSameTeam(p) and C.TeamColor or C.EnemyColor
                            hl.OutlineColor = C.EnemyColor
                            hl.Parent = p.Character
                        elseif not C.Highlight then
                            local hl = p.Character:FindFirstChild("RENXX_Highlight")
                            if hl then hl:Destroy() end
                        end
                        
                        if C.HitboxExpander then
                            for _, part in pairs(p.Character:GetDescendants()) do
                                pcall(function()
                                    if part:IsA("BasePart") and (part.Name == "Head" or part.Name == "HumanoidRootPart" or part.Name == "UpperTorso") then
                                        part.Size = Vector3.new(C.HitboxSize, C.HitboxSize, C.HitboxSize)
                                        part.Transparency = 0.7
                                        part.CanCollide = false
                                        part.Massless = true
                                    end
                                end)
                            end
                        end
                        
                        if C.ESPLine then
                            local l = Drawing.new("Line")
                            l.From = Vector2.new(Cam.ViewportSize.X/2, Cam.ViewportSize.Y)
                            l.To = Vector2.new(sp.X, sp.Y)
                            l.Color = isSameTeam(p) and C.TeamColor or C.EnemyColor
                            l.Thickness = 1; l.Visible = true
                            table.insert(drawings, l)
                        end
                        if C.ESPName then
                            local t = Drawing.new("Text")
                            t.Text = p.Name; t.Position = Vector2.new(sp.X, sp.Y - 40)
                            t.Color = C.TextColor; t.Size = 14; t.Center = true; t.Outline = true; t.Visible = true
                            table.insert(drawings, t)
                        end
                        if C.ESPDistance then
                            local dist = (hrp.Position - Cam.CFrame.Position).Magnitude
                            local t = Drawing.new("Text")
                            t.Text = math.floor(dist) .. "m"; t.Position = Vector2.new(sp.X, sp.Y + 30)
                            t.Color = C.TextColor; t.Size = 12; t.Center = true; t.Outline = true; t.Visible = true
                            table.insert(drawings, t)
                        end
                        if C.ESPHealth then
                            local t = Drawing.new("Text")
                            t.Text = "HP: " .. math.floor(hum.Health) .. "/" .. math.floor(hum.MaxHealth)
                            t.Position = Vector2.new(sp.X, sp.Y + 45)
                            t.Color = C.HealthColor; t.Size = 12; t.Center = true; t.Outline = true; t.Visible = true
                            table.insert(drawings, t)
                        end
                        if C.ESPBox then
                            local b = Drawing.new("Square")
                            b.Size = Vector2.new(50, 80); b.Position = Vector2.new(sp.X - 25, sp.Y - 40)
                            b.Color = C.BoxColor; b.Thickness = 1; b.Filled = false; b.Visible = true
                            table.insert(drawings, b)
                        end
                        if C.ESPSkeleton then
                            local parts = {"Head", "UpperTorso", "LowerTorso", "LeftUpperArm", "RightUpperArm", "LeftLowerArm", "RightLowerArm", "LeftUpperLeg", "RightUpperLeg", "LeftLowerLeg", "RightLowerLeg"}
                            local positions = {}
                            for _, partName in pairs(parts) do
                                local part = p.Character:FindFirstChild(partName)
                                if part then
                                    local psp, pon = Cam:WorldToViewportPoint(part.Position)
                                    if pon then positions[partName] = Vector2.new(psp.X, psp.Y) end
                                end
                            end
                            local connections = {
                                {"Head", "UpperTorso"}, {"UpperTorso", "LowerTorso"},
                                {"UpperTorso", "LeftUpperArm"}, {"LeftUpperArm", "LeftLowerArm"},
                                {"UpperTorso", "RightUpperArm"}, {"RightUpperArm", "RightLowerArm"},
                                {"LowerTorso", "LeftUpperLeg"}, {"LeftUpperLeg", "LeftLowerLeg"},
                                {"LowerTorso", "RightUpperLeg"}, {"RightUpperLeg", "RightLowerLeg"}
                            }
                            for _, conn in pairs(connections) do
                                if positions[conn[1]] and positions[conn[2]] then
                                    local sl = Drawing.new("Line")
                                    sl.From = positions[conn[1]]; sl.To = positions[conn[2]]
                                    sl.Color = C.SkeletonColor; sl.Thickness = 1; sl.Visible = true
                                    table.insert(drawings, sl)
                                end
                            end
                        end
                    end
                end
            end
        end
    end
end)

-- ==================== HEARTBEAT LOOP (PLAYER) ====================
RunService.Heartbeat:Connect(function()
    if LP.Character then
        local h = LP.Character:FindFirstChildOfClass("Humanoid")
        if h then
            if C.GodMode then h.Health = h.MaxHealth end
            if C.SpeedHack then
                if h.WalkSpeed ~= C.SpeedValue then h.WalkSpeed = C.SpeedValue end
            else
                if h.WalkSpeed ~= 16 then h.WalkSpeed = 16 end
            end
            if C.JumpHack then
                h.UseJumpPower = true
                if h.JumpPower ~= C.JumpValue then h.JumpPower = C.JumpValue end
            else
                h.UseJumpPower = true
                if h.JumpPower ~= 50 then h.JumpPower = 50 end
            end
            if C.BunnyHop then
                if h:GetState() == Enum.HumanoidStateType.Landed then
                    h:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end
            if C.Fly then
                local rootPart = LP.Character:FindFirstChild("HumanoidRootPart")
                if rootPart then
                    local moveDir = Vector3.new()
                    if UIS:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + Cam.CFrame.LookVector end
                    if UIS:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - Cam.CFrame.LookVector end
                    if UIS:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - Cam.CFrame.RightVector end
                    if UIS:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + Cam.CFrame.RightVector end
                    if UIS:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
                    if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then moveDir = moveDir - Vector3.new(0, 1, 0) end
                    if moveDir.Magnitude > 0 then
                        rootPart.Velocity = moveDir.Unit * C.FlySpeed
                    else
                        rootPart.Velocity = Vector3.new(0, 0, 0)
                    end
                end
            end
        end
        if C.NoClip then
            for _, p in pairs(LP.Character:GetDescendants()) do
                pcall(function() if p:IsA("BasePart") then p.CanCollide = false end end)
            end
        end
    end
end)

-- ==================== INFINITE JUMP ====================
local function setupInfJump(char)
    local h = char:WaitForChild("Humanoid")
    h.StateChanged:Connect(function(oldState, newState)
        if C.InfJump and newState == Enum.HumanoidStateType.Freefall then
            task.wait(0.1)
            if C.InfJump then h:ChangeState(Enum.HumanoidStateType.Jumping) end
        end
    end)
end

if LP.Character then setupInfJump(LP.Character) end
LP.CharacterAdded:Connect(setupInfJump)

-- ==================== WEAPON LOOP (FIXED) ====================
task.spawn(function()
    while task.wait(0.3) do
        if C.FireRate or C.NoRecoil or C.NoSpread or C.HighRange or C.OneHit or C.ReloadSpeed or C.DamageMultiplier then
            if LP.Character then
                local tool = LP.Character:FindFirstChildOfClass("Tool")
                if tool then
                    for _, obj in pairs(tool:GetDescendants()) do
                        pcall(function()
                            if obj:IsA("NumberValue") or obj:IsA("IntValue") then
                                local n = obj.Name:lower()
                                local val = tonumber(obj.Value)
                                if not val then return end
                                if C.FireRate and (n:find("firerate") or n:find("cooldown") or n:find("delay")) then obj.Value = math.max(0.01, val / C.FireRateValue) end
                                if C.NoRecoil and (n:find("recoil") or n:find("kick")) then obj.Value = 0 end
                                if C.NoSpread and (n:find("spread") or n:find("accuracy")) then obj.Value = 0 end
                                if C.HighRange and (n:find("range") or n:find("distance")) then obj.Value = 99999 end
                                if C.OneHit and (n:find("damage") or n:find("dmg")) then obj.Value = 9999 end
                                if C.ReloadSpeed and n:find("reload") then obj.Value = math.max(0.01, val / C.ReloadSpeedValue) end
                                if C.DamageMultiplier and (n:find("damage") or n:find("dmg")) then obj.Value = val * C.DamageValue end
                            end
                            for attrName, attrVal in pairs(obj:GetAttributes()) do
                                local n = attrName:lower()
                                if type(attrVal) == "number" then
                                    if C.FireRate and (n:find("firerate") or n:find("cooldown")) then obj:SetAttribute(attrName, math.max(0.01, attrVal / C.FireRateValue)) end
                                    if C.NoRecoil and n:find("recoil") then obj:SetAttribute(attrName, 0) end
                                    if C.NoSpread and n:find("spread") then obj:SetAttribute(attrName, 0) end
                                    if C.HighRange and n:find("range") then obj:SetAttribute(attrName, 99999) end
                                    if C.OneHit and n:find("damage") then obj:SetAttribute(attrName, 9999) end
                                    if C.ReloadSpeed and n:find("reload") then obj:SetAttribute(attrName, math.max(0.01, attrVal / C.ReloadSpeedValue)) end
                                    if C.DamageMultiplier and n:find("damage") then obj:SetAttribute(attrName, attrVal * C.DamageValue) end
                                end
                            end
                        end)
                    end
                end
            end
        end
    end
end)

-- ==================== ANTI AFK ====================
LP.Idled:Connect(function()
    if C.AntiAFK then
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
    end
end)

-- ==================== ANTI KICK ====================
if C.AntiKick then
    pcall(function()
        local mt = getrawmetatable(game)
        if mt and setreadonly then
            setreadonly(mt, false)
            local oldNamecall = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                local method = getnamecallmethod()
                if method == "Kick" and self == LP then return end
                return oldNamecall(self, ...)
            end)
            setreadonly(mt, true)
        end
    end)
end

-- ==================== NOTIFIKASI ====================
Fluent:Notify({
    Title = "RENXX HUB V3.5",
    Content = "Super Update! Weapon V1 & V2 Added!",
    Duration = 5
})

print("RENXX HUB V3.5 - All Features Working! (Fluent UI)")
